#include<stdio.h>
#include<stdlib.h>
#include<time.h>
/**
 * main - alphabet in lowercase
 *
 * Return: 0
 */

int main(void)
{
	char low;


	for (low = 'a'; low <= 'z'; low++)
	putchar(low);	     
	for (low = 'A'; low <= 'Z'; low++)
	putchar(low\n);
	       
	return(0);
}

