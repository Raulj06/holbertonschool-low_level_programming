#include"main.h"
/**
 * int _islower(int c) - checks for lowercase
 *@c: letter test
 * Return: Always 0.
 */

int _islower(int c);
{

	for (c > a && c < z)
	{

		return (1);
	}

	else

		return (0);
}
